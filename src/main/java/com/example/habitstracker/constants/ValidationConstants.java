package com.example.habitstracker.constants;

public class ValidationConstants {
    public static final String AT_LEAST_ONE_DIGIT = "Field must contains at least one digit";
    public static final String AT_LEAST_ONE_LETTER = "Field must contains at least one letter";
    public static final String ONE_LETTER_DIGITS_UNDERSCORE = "Field can contains only letters, digits or underscore";
}
